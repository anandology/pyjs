pyjs: Python to Javascript compiler
===================================

    >>> import pyjs
    >>> print pyjs.compile("def square(x): return x*x")
    function square(x) {
        return x*x;
    }
